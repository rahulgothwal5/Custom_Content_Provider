<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <permission android:name="com.example.customcontentprovider.PERMISSION.READ" android:protectionLevel="normal"/>
  <permission android:name="com.example.customcontentprovider.PERMISSION.WRITE" android:protectionLevel="normal"/>

  <application
      android:allowBackup="true"
      android:name=".ContentProviderApp"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.CustomContentProvider"
      tools:targetApi="31">
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.CustomContentProvider">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <provider
        android:name=".contentprovider.ProductContentProvider"
        android:authorities="com.example.customcontentprovider"
        android:exported="true"
        android:readPermission="com.example.customcontentprovider.PERMISSION.READ"
        android:writePermission="com.example.customcontentprovider.PERMISSION.WRITE"/>

  </application>

</manifest>